group = "com.afalphy.audio_tags_lofty"
version = "1.0"

apply plugin: "com.android.library"

android {
    namespace = "com.afalphy.audio_tags_lofty"
    compileSdk = 36

    defaultConfig {
        minSdk = 24
    }
}